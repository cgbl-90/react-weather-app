import React from "react";

export default function printForecast(forecastData) {
  /* 
    I am unable to get the forecast data

    Use methods filter and map together to avoid printing when the forecast is empty

    forecast.filter (day => day.id !== 0). map (...)

  */

  return " ";
}
